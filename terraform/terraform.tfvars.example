# AWS Configuration
aws_region   = "ap-south-1"
project_name = "sks-hospital"
environment  = "dev"

# Database Configuration
db_username = "hospitaladmin"
db_password = "ChangeThisSecurePassword123!"  # IMPORTANT: Change this to a strong password!
db_name     = "hospital_db"

# EC2 Configuration
key_name = "your-ec2-keypair-name"  # Replace with your AWS key pair name

# Network Configuration (Optional - uses defaults if not specified)
# vpc_cidr              = "10.0.0.0/16"
# public_subnet_cidrs   = ["10.0.1.0/24", "10.0.2.0/24"]
# private_subnet_cidrs  = ["10.0.3.0/24", "10.0.4.0/24"]

# Security Configuration
my_ip = "0.0.0.0/0"  # Replace with your IP for better security (e.g., "203.0.113.0/32")

# Instance Types (Optional - uses defaults if not specified)
# instance_type      = "t3.micro"
# db_instance_class  = "db.t3.micro"